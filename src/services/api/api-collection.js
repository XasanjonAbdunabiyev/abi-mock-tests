/* The code is defining an object named `API_COLLECTIONS` with four properties: `FREE_MOCK_TESTS`,
`PURCHASE_MOCK_TESTS`, `LOGIN`, and `USERS`. Each property is assigned a string value. This object
is then exported, making it available for use in other modules. */
export const API_COLLECTIONS = {
	FREE_MOCK_TESTS: 'mock_tests',
	PURCHASE_MOCK_TESTS: 'purchase_mock_tests',
	LOGIN: 'login',
	USERS: 'users',
}
